node_modules
dist
coverage
# Keep environment variables out of version control
.env

/generated/prisma

# Local-disk StorageService output (branding logos, profile photos) — dev/test artifacts
/uploads
